platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 7-jre-alpine

inputs:
# - name: release-branch
- name: code-repo

run:
  # path: sh
  # args: ['./release-branch/ci/scripts/version.sh']
  dir: code-repo
  path: ./scripts/github_version.sh
